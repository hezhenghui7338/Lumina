<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Lumina.Features.Tasks.TaskManagerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:Lumina.Services">
    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Content="← 设置" Click="Back_Click"/>
                <TextBlock Text="任务管理" FontSize="28" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                <Button Content="全部开始摘要" Click="StartAll_Click"/>
                <Button Content="全部停止" Click="StopAll_Click"/>
                <Button Content="刷新" Click="Refresh_Click"/>
            </StackPanel>
        </Grid>
        <TextBlock x:Name="OverviewText" Grid.Row="1" TextWrapping="WrapWholeWords" Margin="0,0,0,8"/>
        <TextBlock x:Name="RuntimeText" Grid.Row="2" FontSize="12"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   TextWrapping="WrapWholeWords" Margin="0,0,0,8"/>
        <ListView x:Name="TasksList" Grid.Row="3">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:OpsTask">
                    <Grid Padding="8,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock FontWeight="SemiBold" Text="{x:Bind SubjectLabel}"/>
                            <TextBlock FontSize="12" Text="{x:Bind Detail}" TextWrapping="WrapWholeWords"/>
                            <TextBlock FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                <Run Text="{x:Bind Kind}"/>
                                <Run Text=" · "/>
                                <Run Text="{x:Bind Status}"/>
                            </TextBlock>
                        </StackPanel>
                        <Button Grid.Column="1" Content="取消" Tag="{x:Bind Id}" Click="Cancel_Click"
                                VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
