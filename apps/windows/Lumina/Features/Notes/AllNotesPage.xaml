<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Lumina.Features.Notes.AllNotesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:Lumina.Services">
    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Content="← 书库" Click="Back_Click"/>
                <TextBlock Text="全部笔记" FontSize="28" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                <Button Content="删除所选" Click="DeleteSelected_Click"/>
                <Button Content="刷新" Click="Refresh_Click"/>
            </StackPanel>
        </Grid>
        <TextBlock x:Name="StatusText" Grid.Row="1" Margin="0,0,0,8"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        <ListView x:Name="NotesList" Grid.Row="2"
                  SelectionMode="Multiple"
                  IsItemClickEnabled="True"
                  ItemClick="Notes_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:NoteRow">
                    <StackPanel Padding="8,10">
                        <TextBlock FontWeight="SemiBold" TextWrapping="WrapWholeWords"
                                   Text="{x:Bind BookTitle}"/>
                        <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Text="{x:Bind SegmentLabel}"/>
                        <TextBlock Margin="0,4,0,0" TextWrapping="WrapWholeWords" Text="{x:Bind Content}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
